#!/usr/bin/env sh
# Run the versioned, no-credential baseline fixture in the Docker-only runtime.
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" --no-network --non-interactive --load scripts/run-baseline.lisp
