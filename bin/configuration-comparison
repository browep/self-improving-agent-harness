#!/usr/bin/env sh
# Run the deterministic no-provider configuration comparison in Docker.
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" --writable-workspace --no-network --non-interactive \
  --load scripts/run-configuration-comparison.lisp
