#!/usr/bin/env sh
# Load and print the offline experiment DSL example in the Docker-only runtime.
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" \
  --no-network \
  --non-interactive \
  --load scripts/experiment-example.lisp
