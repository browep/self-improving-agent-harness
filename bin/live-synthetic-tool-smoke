#!/usr/bin/env sh
# Execute one real Synthetic OpenAI-compatible tool-call loop in Docker.
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" \
  --non-interactive \
  --load scripts/live-synthetic-tool-smoke.lisp