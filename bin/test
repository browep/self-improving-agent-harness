#!/usr/bin/env sh
# Execute the full ASDF test system in the portable container runtime.
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
"$repo_root/bin/container" \
  --no-network \
  --non-interactive \
  --load scripts/run-tests.lisp
"$repo_root/tests/claude-shim-bridge.sh"
