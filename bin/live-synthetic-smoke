#!/usr/bin/env sh
# Execute one real, minimal Synthetic OpenAI-compatible request in Docker.
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" \
  --non-interactive \
  --load scripts/live-synthetic-smoke.lisp